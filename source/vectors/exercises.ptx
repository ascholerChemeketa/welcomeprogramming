<exercises xml:id="vectors_exercises"
           xmlns:xi="http://www.w3.org/2001/XInclude">

  <title>Exercises</title>

  <exercise label="vectors_exercises-ex-1">
    <statement>
      <p>Create a vector of strings called <pf>words</pf> that contains "Oregon" and "Washington". Then read in three more words (using <pf>cin >></pf> into a string variable you create) and add them to the end of the vector.</p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
]]>
        </preamble>
        <code>

    //write your code here

        </code>
        <postamble visible="yes"><![CDATA[
    for(const string& s : words) {
        cout << s << " ";
    }
}]]></postamble>
        <tests>
          <iotest>
            <input>Wisconsin Colorado Florida</input>
            <output>Oregon Washington Wisconsin Colorado Florida</output>
          </iotest>
          <iotest>
            <input>Iowa Vermont Arizona</input>
            <output>Oregon Washington Iowa Vermont Arizona</output>
          </iotest>
        </tests>
    </program>
  </exercise>

  
  <exercise label="vectors_exercises-ex-2">
    <statement>
      <p>Write the function <pf>void removeLastHalf(vector&lt;int>&amp; vec);</pf> that removes the last half of the vector. If the size is odd, round up the number of items to keep (so for a 5 item list you should keep the first 3 items and remove the last two).</p>
      <p>Note that the function takes a reference, so you should make changes to <pf>vec</pf> and not try to return anything.</p>
      <p>For example:
      </p>
      <pre>
Input: {1, 2, 3, 4, 5, 6}
Result: {1, 2, 3}

Input: {10, 20, 30}
Result: {10, 20}
      </pre>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <string>
#include <vector>
using namespace std;

void removeLastHalf(vector<int>& vec) {
]]>
        </preamble>
        <code>

    //write your code here

        </code>
        <postamble visible="yes"><![CDATA[
}

]]></postamble>
        <tests visible="yes">
<![CDATA[

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("removeLastHalf") {
  vector<int> vec1 = {1, 2, 3, 4};
  removeLastHalf(vec1);
  CHECK(vec1 == vector<int>({1, 2}));
  vector<int> vec2 = {100, 200, 300};
  removeLastHalf(vec2);
  CHECK(vec2 == vector<int>({100, 200}));
  vector<int> vec3 = {10, 9, 8, 7, 6, 5, 4};
  removeLastHalf(vec3);
  CHECK(vec3 == vector<int>({10, 9, 8, 7}));
}]]>
        </tests>
    </program>
  </exercise>


  <exercise label="vectors_exercises-ex-3">
    <statement>
      <p>Write the function <pf>void swapEnds(vector&lt;int>&amp; vec);</pf> that swaps the first and last items of the given vector.</p>
      <p>For example:
      </p>
      <pre>
Input: {1, 2, 3, 4, 5}
Returns: {5, 2, 3, 4, 1}

Input: {10, 20, 30}
Output: {30, 20, 10}
      </pre>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
using namespace std;

]]>
        </preamble>
        <code><![CDATA[
void swapEnds(vector<int>& vec) {
    // write swapEnds
}
]]>
        </code>
        <tests visible="yes">
<![CDATA[

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("swapEnds") {
  vector<int> vec1 = {1, 2, 3, 4};
  swapEnds(vec1);
  CHECK(vec1 == vector<int>({4, 2, 3, 1}));
  vector<int> vec2 = {100, 200, 300};
  swapEnds(vec2);
  CHECK(vec2 == vector<int>({300, 200, 100}));
  vector<int> vec3 = {10, 9, 8, 7, 6, 5, 4};
  swapEnds(vec3);
  CHECK(vec3 == vector<int>({4, 9, 8, 7, 6, 5, 10}));
}]]>
        </tests>
    </program>
    <hint><p>Remember that the index of the last item is always <pf>vec.size() - 1</pf></p></hint>
    <hint><p>You will need to use a temporary variable to hold a value while swapping the first and last elements.</p></hint>
  </exercise>

  
  <exercise label="vectors_exercises-ex-4">
    <statement>
      <p>Write the function <pf>vector&lt;int> swapEnds(const vector&lt;int>&amp; original);</pf> that copies the given vector and then swaps the first and last items of the copy before returning the copy.</p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <string>
#include <vector>
using namespace std;

]]>
        </preamble>
        <code>
<![CDATA[
vector<int> swapEnds(const vector<int>&amp; original) {
    //write your code here
}
    ]]>
        </code>
        <tests visible="yes">
<![CDATA[

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("swapEnds") {
  vector<int> vec1 = {1, 2, 3, 4};
  vector<int> vec1b = swapEnds(vec1);
  CHECK(vec1b == vector<int>({4, 2, 3, 1}));

  vector<int> vec2 = {100, 200, 300};
  vector<int> vec2b = swapEnds(vec2);
  CHECK(vec2b == vector<int>({300, 200, 100}));

  vector<int> vec3 = {10, 9, 8, 7, 6, 5, 4};
  vector<int> vec3b = swapEnds(vec3);
  CHECK(vec3b == vector<int>({4, 9, 8, 7, 6, 5, 10}));
}]]>
        </tests>
    </program>
    <hint><p>This vector is passed by const reference, so we can't make changes to the original vector.</p></hint>
    <hint><p>You will need to use a temporary variable to hold a value while swapping the first and last elements.</p></hint>
  </exercise>


  <exercise label="vectors_exercises-ex-5">
    <statement>
      <p>Complete the function <pf>trimZeros</pf> that removes values from the end of <pf>vec</pf> until the last value is not 0.</p>
      <p>For example:
      </p>
      <pre>
Input: {1, 4, 0, 0, 0}
Result: {1, 4}

Input: {1, 8, 3, 0, 2, 0}
Result: {1, 8, 3, 0, 2}
      </pre>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
using namespace std;
]]>
        </preamble>
        <code><![CDATA[
void trimZeros(vector<int>& vec) {
    //write your code here
}]]>
        </code>
        <tests visible="yes">
<![CDATA[
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("trimZeros") {
  vector<int> vec1 = {1, 4, 0, 0, 0};
  trimZeros(vec1);
  CHECK(vec1 == vector<int>({1, 4}));
  vector<int> vec2 = {1, 8, 3, 0, 2, 0};
  trimZeros(vec2);
  CHECK(vec2 == vector<int>({1, 8, 3, 0, 2}));
  vector<int> vec3 = {1, 6, 2};
  trimZeros(vec3);
  CHECK(vec3 == vector<int>({1, 6, 2}));
}]]>
        </tests>
    </program>
    <hint><p>This is not really a traversal problem. Yes, you will need a loop, but you do not need to visit every element.</p></hint>
    <hint><p>You will need a loop based on <q>what is the value of the last element</q>. Remember that the last element is at index <pf>.size() - 1</pf> and changes each time you remove something from the back.</p></hint>
  </exercise>

  <exercise label="vectors_exercises-ex-6">
    <statement>
      <p>Complete the function <pf>maximumValue</pf> that returns the largest value in <pf>vec</pf></p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
using namespace std;
]]>
        </preamble>
        <code><![CDATA[
double maximumValue(const vector<double>& vec) {
    //write your code here
}
]]>
        </code>
        <tests visible="yes">
<![CDATA[
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("maximumValue") {
  vector<double> vec1 = {4.2, 3.1, 2.5, 1.2};
  CHECK(maximumValue(vec1) == doctest::Approx(4.2));
  vector<double> vec2 = {4.2, -6,1, 3.1, 12.5, 1.2};
  CHECK(maximumValue(vec2) == doctest::Approx(12.5));
  vector<double> vec3 = {4.2, -6,1, 3.1, 12.5, 111.2};
  CHECK(maximumValue(vec3) == doctest::Approx(111.2));
}]]>
        </tests>
    </program>
  </exercise>

  <exercise label="vectors_exercises-ex-7">
    <statement>
      <p>Complete the function <pf>countOver</pf> that returns the number of values greater than <pf>value</pf>.</p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
using namespace std;
]]>
        </preamble>
        <code><![CDATA[
int countOver(const vector<int>& vec, double value) {
    //write your code here
}
]]>
        </code>
        <tests visible="yes">
<![CDATA[
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("minimumVal") {
  vector<double> temps = {98.6, 97.8, 100.3, 97.2, 98.7, 97.8, 99.8, 96.9, 98.2, 99.1, 99.9};
  CHECK(countOver(temps, 98) == 8);
  CHECK(countOver(temps, 99) == 4);
  CHECK(countOver(temps, 100) == 1);
  CHECK(countOver(temps, 102) == 0);
  CHECK(countOver(temps, 98.6) == 5);
}]]>
        </tests>
    </program>
  </exercise>

  <exercise label="vectors_exercises-ex-8">
    <statement>
      <p>Write a function <pf>isBlankFree</pf> that takes a vector of strings and returns <pf>true</pf> if there are no empty strings (<pf>""</pf>) in it. Return <pf>false</pf> if there is at least one empty string.</p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
#include <string>
using namespace std;
]]>
        </preamble>
        <code><![CDATA[

// write isBlankFree

]]>
        </code>
        <tests visible="yes">
<![CDATA[
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("isBlankFree") {
  vector<string> test1 = {"he", "llo", "world", "", "!"};
  CHECK(!isBlankFree(test1));
  vector<string> test2 = {"he", "llo", "world", "!"};
  CHECK(isBlankFree(test2));
  vector<string> test3 = {"", "llo", "world", "!"};
  CHECK(!isBlankFree(test3));
  vector<string> test4 = {"he", "", "llo", "", "world", "!"};
  CHECK(!isBlankFree(test4));
}]]>
        </tests>
    </program>
  </exercise>

  
  <exercise label="vectors_exercises-ex-9">
    <statement>
      <p>Write a function <pf>limit</pf> that takes a vector of integers and returns a new vector that is a copy of the original where every value less than 0 is set to 0 and every value greater than 100 is set to 100.</p>
      <p>For example, passing in <pf>54 -6 82 100 102</pf> would result in <pf>54 0 82 100 100</pf>.</p>
      </statement>
      <program interactive="activecode">
        <preamble visible="yes"><![CDATA[
#include <iostream>
#include <vector>
using namespace std;
]]>
        </preamble>
        <code><![CDATA[

// write limit

]]>
        </code>
        <tests visible="yes">
<![CDATA[
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

TEST_CASE("limit") {
  vector<int> test1 = {54, -6, 82, 100, 102};
  vector<int> result1 = limit(test1);
  CHECK(result1 == vector<int>({54, 0, 82, 100, 100}));
  vector<int> test2 = {-4, 0, -123};
  vector<int> result2 = limit(test2);
  CHECK(result2 == vector<int>({0, 0, 0}));
  vector<int> test3 = {62, 102, 103, 18};
  vector<int> result3 = limit(test3);
  CHECK(result3 == vector<int>({62, 100, 100, 18}));
}]]>
        </tests>
    </program>
  </exercise>


</exercises>