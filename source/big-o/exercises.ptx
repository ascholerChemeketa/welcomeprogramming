<exercises xml:id="big-o_exercises"
           xmlns:xi="http://www.w3.org/2001/XInclude">

  <title>Exercises</title>

  <exercise label="big-o_exercises-ex-1">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void foo(int n) {
  for (int i = 0; i < n - 5; i++) {
    cout << i << endl;
  }
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-2">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void foo(int n) {
  for (int i = 0; i < n; i += 2) {
    cout << i << endl;
  }

  cout << "----" << endl;

  for (int j = 0; j < n / 2; j++) {
    cout << j << endl;
  }

  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-3">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void foo(int n) {
  for (int i = 0; i < n; i++) {
    for (int j = i; j < n; j++) {
      cout << i << ", " << j << endl;
    }
  }
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-4">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void foo(int n) {
  cout << "----" << endl;
  for (int i = 0; i < 5; i++) {
    for (int j = i; j < n; j *= 2) {
      cout << i << ", " << j << endl;
    }
  }
  cout << "----" << endl;
  int k = n;
  while (k > 0) {
    cout << k << endl;
    k--;
  }
  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-5">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
      <p>Assume <pf>bar</pf> is known to run in <m>O(n)</m> time.</p>
<program>
<![CDATA[
void foo(int n) {
  cout << "----" << endl;
  bar(n);
  bar(n / 2);
  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-6">
    <statement>
      <p>Give the Big-O notation for the following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
      <p>Assume <pf>bar</pf> is known to run in <m>O(n)</m> time.</p>
<program>
<![CDATA[
void foo(int n) {
  cout << "----" << endl;
  for (int i = 0; i < n * 2; i++) {
    bar(i);
  }
  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-7">
    <statement>
      <p>Give the Big-O notation for <pf>foo</pf> following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void bar(int n) {
  cout << n << endl;
}

void foo(int n) {
  cout << "----" << endl;
  for (int i = 0; i < 100; i++) {
    bar(i);
  }
  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>

  <exercise label="big-o_exercises-ex-8">
    <statement>
      <p>Give the Big-O notation for <pf>foo</pf> following function. Explain your reasoning. Make sure to describe how each loop and function call contributes to the overall complexity.</p>
<program>
<![CDATA[
void bar(int n) {
  for(int i = 0; i < n; i++) {
    cout << "*";
  }
}

void foo(int n) {
  cout << "----" << endl;
  for (int i = 0; i < n; i++) {
    bar(2 * n);
  }
  cout << "----" << endl;
}
]]>
</program>
    </statement>
    <response>
    </response>
  </exercise>


</exercises>