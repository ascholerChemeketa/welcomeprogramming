#include <iostream>
#include <cmath>     // need to include for sqrt()

using namespace std;

int main() {
    cout << "1.0 / 0.0 is " << (1.0 / 0.0) << endl;
    cout << "sqrt(-1.0) is " << sqrt(-1.0) << endl;
}