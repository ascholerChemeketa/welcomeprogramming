#include <iostream>
using namespace std;

int main() {
    cout << "'A' == 'A'" << ('A' == 'A') << endl;
    cout << "'A' == 'C'" << ('A' == 'C') << endl;
    cout << "'A' == 'a'" << ('A' == 'a') << endl;
    cout << "'B' < 'A'" << ('B' < 'A') << endl;
    cout << "'B' < 'a'" << ('B' < 'a') << endl;
}