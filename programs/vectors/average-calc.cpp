
// Paste your minValue function here

double avgWithoutLow(const vector<double>& scores) {
    // FIXME
    // get the minimum value and store in minScore
    // get the sum and store in totalScore

    size_t numScores = scores.size();
    return (totalScore - minScore) / (numScores - 1);
}