#include <iostream>
#include <string>

using namespace std;

int main() {
    // Allocate an array of 100 integers
    int* data = new int[100]; 

    // Use the array...

    // Delete the array
    delete[] data;
}