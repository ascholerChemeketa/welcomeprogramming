#include <iostream>

// Needed for the sin and cos functions
#include <cmath>

using namespace std;

int main() {
    double theta = 45;

    double sinValue = sin(theta);
    cout << "sinValue: " << sinValue << endl;
}