#include <iostream>
using namespace std;

int main() {
    int a = 5;
    int b = a;   // b gets the value that a has (5)
    // a and b are now equal
    a = 3;       // a gets the value 3
    // a and b are no longer equal. b is still 5
}