#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double x = 2.5;
    double result = pow(x, 3);
    cout << "The result of " << x << " to the 3rd power is " << result << endl;
}