<instructions xmlns:xi="http://www.w3.org/2001/XInclude">
  <p>
    When the <c> > </c> button is highlighted, an animation is prepared to run. Use the Step button to step through the animation one step at a time.
  </p>
  <p>When no animation is in progress, you can use the data controls to start a new animation.</p>
  <p>You can click and drag on the animation area to pan around. Use the Ctrl + mouse wheel (or touchpad scroll gesture) while over the animation area to zoom in and out.</p>
<p>
  <ul>
<xi:include href="ds-core-controls.xml"/>
    <li>
      <title>
        Graph Controls
      </title>
      <p>
        <ul>
          <li><term>New Graph</term> Randomly generate a new graph with the same settings.</li> 
          <li><term>Directed Graph</term> Randomly generate a new directed graph.</li> 
          <li><term>Undirected Graph</term> Randomly generate a new undirected graph.</li> 
          <li><term>Small Graph</term> Randomly generate a graph with 8 vertices.</li> 
          <li><term>Large Graph</term> Randomly generate a graph with 19 vertices.</li> 
          <li><term>Logical Representation</term> Display current graph's logical representation.</li> 
          <li><term>Adjacency List Representation</term> Display current graph's adjacency list representation.</li>
          <li><term>Adjacency Matrix Representation</term> Display current graph's adjacency matrix representation.</li> 
        </ul>
      </p>
    </li>
  </ul>
</p>
</instructions>